---
name: planner
description: Use this skill when asked to create, refine, or evaluate an implementation plan, roadmap, migration plan, or structured sequence of work before coding — including any multi-step or multi-session task that needs a written handoff artifact.
---

# Planner

This is a pointer. The canonical planning rules live in `docs/`.

Read these files before acting:

1. `docs/skills/global-project-rules/SKILL.md`
2. `docs/skills/planner/SKILL.md`
3. `docs/skills/planner/planner.md` — the required plan format
4. `docs/workflow.md` — the verification commands each phase must name

Supporting reference:

- `docs/skills/planner/SETUP.md` — recorded planning decisions

Save every plan as `docs/plans/<short-kebab-topic>.md` and add it to the index in
`docs/plans/README.md`. Never leave a plan only in chat.
